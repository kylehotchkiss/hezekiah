machine:
  timezone:
    America/New_York

general:
  branches:
    only:
      - three

dependencies:
  pre:
    - gem install ultrahook

deployment:
  staging:
    branch: three
    heroku:
      appname: hezekiah-staging
